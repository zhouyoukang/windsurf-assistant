# 用户扰动注入文件
# 
# 使用方法:
# 1. Agent长运行时，用户在此文件中写入方向修正/新需求
# 2. Agent在每个检查点(每3-5个tool calls)读取此文件
# 3. Agent吸收内容后清空此文件
# 4. 零积分成本，不阻塞Agent执行
#
# 格式: 直接写入纯文本即可
# 例如: "停止当前方向，转向研究X"
# 例如: "优先级调整：先完成Y再做Z"
#
# 当前状态: 空(无新指令)
